Wrapper around ddc-lib to control external monitors

- Daemon runs in the background that (re)-detects new monitor layouts (e.g. plugged into a dock)
- Control cli-app that sends control commands to running daemon